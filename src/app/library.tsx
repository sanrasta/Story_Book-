import { LibraryScreen } from '../features/library';

/**
 * Library Route
 * Thin route that renders the Library screen from the features module
 */
export default function LibraryRoute(): React.ReactElement {
  return <LibraryScreen />;
}

