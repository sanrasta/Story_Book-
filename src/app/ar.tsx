import { ArScreen } from '../features/ar';

/**
 * AR Route
 * Thin route that renders the AR screen from the features module
 */
export default function ArRoute(): React.ReactElement {
  return <ArScreen />;
}

